// File: App/ContentView.swift
// Phase 1 のファイル構成との互換用。現在の実装ルートをそのまま包む。

import SwiftUI

struct ContentView: View {
    var body: some View {
        AppRootView()
    }
}

#Preview {
    ContentView()
}
