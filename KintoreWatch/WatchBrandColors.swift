// Phase 8/9: iPhone の `AppTheme.accent`（ライト）と同一 RGB。`KintoreWidget` の `widgetAccentForeground` と同期すること。

import SwiftUI

enum WatchBrandColors {
    static let accent = Color(red: 0.29, green: 0.56, blue: 0.85)
}
